node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Local DB
local.db
db.sqlite

# Env
.env
.env.*
!.env.example
!.env.test
.dev.vars
.dev.vars.*

# Filled-in secrets for `wrangler secret bulk` (only secrets.template.json is tracked)
secrets.json
secrets.*.json
.secrets.json
.secrets.*.json
!secrets.template.json

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

*storybook.log
storybook-static
test-results.json

# Migration
scripts/.id-map.json
# Transient D1 sync artifacts (contain production data — never commit)
d1-seed.sql
d1-seed-ordered.sql
d1-delete.sql

# Coverage
coverage/

# Local email previews (pnpm email:preview)
.email-preview/

# Claude Code
.claude/worktrees/
# Sentry Config File
.env.sentry-build-plugin

# Docs-drift detector artifacts (ephemeral)
docs-drift-report.json
changed-files.txt

# tsx runner temp cache (created during build/prepare)
tsx-*/

# Screen-handoff capture output — regenerated by scripts/handoff/, not reviewed
docs/handoff/**/capture-report.json
