# feature/unified-messages — phases

- [x] #1252 group chat: channel, service, remote, band + club routes, nav, seed
- [x] union query `listUnifiedConversations` / `countUnifiedUnread`
- [x] panel-aware topbar messages button
- [x] `readableBy` split: chat = any active member, enquiries = owner/admin
- [x] inbox selector + per-row inbox label on ConversationList
- [x] `/band/[slug]/messages` as a pre-filtered view of the same list
- [x] `/member/messages/[id]` resolves group threads; reply as the group
- [x] `appChrome.messagesUnread` -> `countUnifiedUnread`
- [x] pane scaffold (#1221 rescoped): the three panes agree on height/scroll
- [x] notification on a new chat message (no event emitted today)
