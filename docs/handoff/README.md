# handoff

Material prepared for someone outside the repo — press, partners, funders.

Written to be read by people who will never open the codebase, which is why it lives apart from
`architecture/` and `development/`. It goes stale the way any external-facing copy does: check the
figures against the app before sending anything from here.

<!-- docs-index: delegated -->
