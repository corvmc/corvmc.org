# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/drizzle/

# Postmark template sources — pushed to Postmark via `pnpm email:push`.
# Whitespace inside Mustachio blocks is significant; don't let Prettier reflow it.
/postmark/

# Generated drizzle migrations (SQL + snapshot.json)
/migrations/

# Design-system exploration assets (scratch CSS, chat logs, screenshots)
/design-system/

# Generated by `wrangler types`
worker-configuration.d.ts

# Per-machine Claude Code settings. Gitignored, written by the tool rather than
# by hand, and reformatting it would be a diff nobody asked for — but Prettier
# does not read .gitignore, so it has to be named here or `pnpm lint` fails on
# whatever the tool last wrote.
.claude/settings.local.json
