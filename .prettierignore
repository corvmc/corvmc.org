# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/drizzle/

# Postmark template sources — pushed to Postmark via `pnpm email:push`.
# Whitespace inside Mustachio blocks is significant; don't let Prettier reflow it.
/postmark/

# Generated drizzle migrations (SQL + snapshot.json)
/migrations/

# Design-system exploration assets (scratch CSS, chat logs, screenshots)
/design-system/

# Generated by `wrangler types`
worker-configuration.d.ts

# Per-developer Claude Code settings. Ignored via the *global* gitignore
# (~/.config/git/ignore), not the repo's, so eslint's includeIgnoreFile skips it
# but prettier would otherwise fail `pnpm lint` on every machine that has one.
.claude/settings.local.json
